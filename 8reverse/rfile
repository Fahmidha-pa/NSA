exit 0
cat rfile
done
mv temp file
head -n $l file > temp
l=`expr $l - 1`
tail -n 1 file >> rfile
do
while [ $l -ge 1 ]
rm rfile
l=`cat file | wc -l`
#l=wc -l  file | cut -c 1,1`
cp $1 file

fi 
 exit 1
 echo "Syntax is <filename>"
then
if [ $# -ne 1 ]
#set -xv


#to reverse a file
